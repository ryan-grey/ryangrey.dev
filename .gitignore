.DS_Store

# Local agent config — not part of the site
.claude/

# Project notes: contains AWS account ID, distribution ID, cert ARN,
# hosted zone ID and IAM user. Deliberately not published.
CLAUDE.md

# Build artifact: copied from infra/aif-c01-course.json at deploy/test time.
# One source of truth; this copy just rides along in the Lambda package.
infra/quiz/aif-c01-course.json
