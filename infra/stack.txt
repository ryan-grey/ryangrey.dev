# Stack chips for the "This Website" project card in index.html.
#
# The deploy workflow fails if this list and the card's chip row disagree
# in either direction. Adding a service here without adding the chip, or
# adding a chip without listing it here, both stop the deploy.
#
# One token per line. Must match the chip text exactly. Order here is the
# order they appear on the card. Blank lines and # comments are ignored.

S3
CloudFront
Route 53
ACM
DNSSEC
GitHub OIDC
CloudFront Functions
Lambda
SES
EventBridge
